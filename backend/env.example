# ==================== 資料庫設定 ====================
# 直連 Supabase Postgres（使用 psycopg async driver）
# 格式: postgresql+psycopg://postgres:[YOUR-PASSWORD]@[HOST]:[PORT]/postgres
DATABASE_URL=postgresql+psycopg://postgres:your_password@db.xxxxx.supabase.co:5432/postgres

# psycopg (psycopg3) 支援 async 且與 PgBouncer 完全相容（不使用 prepared statements）

# ==================== JWT 設定 ====================
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ==================== 應用程式設定 ====================
DEBUG=true
APP_NAME=200ok Backend API
APP_VERSION=1.0.0

# ==================== CORS 設定 ====================
# 前端 URL（逗號分隔）
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# ==================== Email 設定 (Resend) ====================
RESEND_API_KEY=re_xxxxx
RESEND_FROM_EMAIL=noreply@200ok.tw

# 前端 URL（用於生成驗證連結）
FRONTEND_URL=http://localhost:3000

# ==================== 代幣系統設定 ====================
TOKEN_UNLOCK_DIRECT_COST=200
TOKEN_SUBMIT_PROPOSAL_COST=100
TOKEN_VIEW_PROPOSAL_COST=50
TOKEN_NEW_USER_GIFT=1000

# ==================== AI 服務設定 (Google Gemini) ====================
# 取得 API Key: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

