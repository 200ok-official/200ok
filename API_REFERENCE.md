# 200 OK API 參考文件

## 🎯 總覽

本文件提供 200 OK 軟體接案平台的完整 API 參考。所有 API 端點都位於 `/api/v1` 路徑下。

**Base URL:** `http://localhost:3000/api/v1` (開發環境)

---

## 🔐 認證 (Authentication)

所有需要認證的端點都需要在 Header 中提供 JWT Token：

```
Authorization: Bearer <your_access_token>
```

### 端點列表

#### POST `/auth/register` - 使用者註冊
#### POST `/auth/login` - 使用者登入
#### POST `/auth/logout` - 使用者登出
#### POST `/auth/refresh` - 刷新 Token
#### POST `/auth/verify-phone` - 驗證手機號碼

---

## 👤 使用者 (Users)

### GET `/users/me` - 取得目前使用者資料
**需要認證**

**回應範例:**
```json
{
  "success": true,
  "data": {
    "id": "uuid",
    "name": "王小明",
    "email": "user@example.com",
    "roles": ["client", "freelancer"],
    "rating": 4.5,
    "skills": ["React", "Node.js"]
  }
}
```

### PUT `/users/me` - 更新使用者資料
**需要認證**

### GET `/users/:id` - 取得使用者公開資料

### POST `/users/me/avatar` - 上傳大頭照
**需要認證**

### PUT `/users/me/password` - 修改密碼
**需要認證**

### PUT `/users/me/skills` - 更新技能標籤
**需要認證**

### GET `/users/:id/reviews` - 取得使用者評價

### GET `/users/:id/stats` - 取得使用者統計資訊

### GET `/users/search` - 搜尋使用者（接案者）

---

## 📋 案件 (Projects)

### GET `/projects` - 取得案件列表
**查詢參數:**
- `status` - 案件狀態 (open, in_progress, completed)
- `skills` - 技能標籤 (逗號分隔)
- `tags` - 標籤 slugs (逗號分隔)
- `budget_min` - 最低預算
- `budget_max` - 最高預算
- `keyword` - 關鍵字搜尋
- `page` - 頁碼 (預設: 1)
- `limit` - 每頁筆數 (預設: 10)

**回應範例:**
```json
{
  "success": true,
  "data": {
    "projects": [...],
    "pagination": {
      "page": 1,
      "limit": 10,
      "total": 50,
      "total_pages": 5
    }
  }
}
```

### POST `/projects` - 建立新案件
**需要認證**

**請求範例:**
```json
{
  "title": "電商網站開發",
  "description": "需要開發一個電商網站...",
  "budget_min": 80000,
  "budget_max": 150000,
  "required_skills": ["React", "Node.js"],
  "tag_ids": ["uuid1", "uuid2"],
  "deliverables": ["原始碼", "部署文件"]
}
```

### GET `/projects/:id` - 取得案件詳情

### PUT `/projects/:id` - 更新案件
**需要認證，只有發案者可以更新**

### DELETE `/projects/:id` - 刪除案件
**需要認證，只有草稿狀態可以刪除**

### GET `/projects/me` - 取得我的案件
**需要認證**

### GET `/projects/saved` - 取得收藏的案件
**需要認證**

### POST `/projects/:id/save` - 收藏案件
**需要認證**

### DELETE `/projects/:id/save` - 取消收藏
**需要認證**

### POST `/projects/:id/publish` - 發布案件
**需要認證，從草稿變為 open 狀態**

### POST `/projects/:id/cancel` - 取消案件
**需要認證**

---

## 💰 投標 (Bids)

### GET `/projects/:id/bids` - 取得案件的所有投標
**需要認證，只有發案者可以查看**

### POST `/projects/:id/bids` - 建立投標
**需要認證**

**請求範例:**
```json
{
  "proposal": "我有 5 年的電商網站開發經驗...",
  "bid_amount": 100000
}
```

### GET `/bids/:id` - 取得投標詳情
**需要認證**

### PUT `/bids/:id` - 更新投標
**需要認證，只有 pending 狀態可以更新**

### DELETE `/bids/:id` - 撤回投標
**需要認證，只有 pending 狀態可以撤回**

### POST `/bids/:id/accept` - 接受投標
**需要認證，只有發案者可以接受**

### POST `/bids/:id/reject` - 拒絕投標
**需要認證，只有發案者可以拒絕**

### GET `/bids/me` - 取得我的投標
**需要認證**

**查詢參數:**
- `status` - 投標狀態 (pending, accepted, rejected)
- `page` - 頁碼
- `limit` - 每頁筆數

---

## 💬 訊息 (Messages)

### GET `/projects/:id/messages` - 取得案件的訊息列表
**需要認證**

### POST `/projects/:id/messages` - 發送訊息
**需要認證**

**請求範例:**
```json
{
  "content": "您好，關於這個案件...",
  "receiver_id": "uuid",
  "attachment_urls": []
}
```

### PUT `/messages/:id/read` - 標記訊息為已讀
**需要認證**

### GET `/messages/conversations` - 取得所有對話列表
**需要認證**

### GET `/messages/unread-count` - 取得未讀訊息數量
**需要認證**

---

## ⭐ 評價 (Reviews)

### GET `/projects/:id/reviews` - 取得案件的評價

### POST `/projects/:id/reviews` - 建立評價
**需要認證，案件必須已完成**

**請求範例:**
```json
{
  "rating": 5,
  "comment": "合作愉快，專業且準時",
  "tags": ["專業", "準時", "溝通良好"]
}
```

### GET `/projects/:id/can-review` - 檢查是否可以評價
**需要認證**

### GET `/users/:id/reviews/stats` - 取得使用者的評價統計

---

## 🛡️ 管理員 (Admin)

### GET `/admin/users` - 取得所有使用者列表
**需要管理員權限**

### POST `/admin/users/:id/ban` - 停權使用者
**需要管理員權限**

### GET `/admin/projects` - 取得所有案件列表
**需要管理員權限**

### POST `/admin/projects/:id/remove` - 下架違規案件
**需要管理員權限**

### GET `/admin/stats` - 取得平台統計資訊
**需要管理員權限**

### GET `/admin/activity` - 取得最近活動
**需要管理員權限**

### GET `/admin/tags/stats` - 取得標籤統計
**需要管理員權限**

---

## 📊 回應格式

### 成功回應
```json
{
  "success": true,
  "data": { ... },
  "message": "操作成功" // 可選
}
```

### 錯誤回應
```json
{
  "success": false,
  "error": {
    "message": "錯誤訊息",
    "code": "ERROR_CODE",
    "details": { ... } // 可選
  }
}
```

---

## 🔑 常見錯誤代碼

- `400` - Bad Request：請求參數錯誤
- `401` - Unauthorized：未登入或 Token 無效
- `403` - Forbidden：沒有權限
- `404` - Not Found：資源不存在
- `409` - Conflict：資源衝突
- `429` - Too Many Requests：請求過於頻繁
- `500` - Internal Server Error：伺服器錯誤

---

## 📝 備註

1. 所有時間格式為 ISO 8601 (例如: `2024-01-15T10:30:00Z`)
2. 所有金額單位為新台幣 (TWD)
3. 分頁預設每頁 10 筆，最多 100 筆
4. API Rate Limit：每分鐘 60 次請求

---

**最後更新：** 2024-01-20
**版本：** v1.0.0

